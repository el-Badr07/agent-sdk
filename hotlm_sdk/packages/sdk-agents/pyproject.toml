[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hotlm-agents"
version = "0.0.1"
description = "Agent orchestration and execution for the HotLM SDK"
readme = "README.md"
license = "MIT"
requires-python = ">=3.9"

dependencies = [
    "hotlm-core>=0.0.1",
]
